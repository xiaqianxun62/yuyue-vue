# 校园羽毛球球局系统官网 - 需求拆解文档

## 产品概述

- **产品类型**: 产品官网 / 营销落地页（带实时交互演示区）
- **场景类型**: <scene_type>prototype-app</scene_type>
- **目标用户**: 全校羽毛球爱好者（学生/教职工），尤其是没有固定球搭子、想找势均力敌对手的人群
- **核心价值**: 通过 ELO 积分沉淀战绩 + 自动编排引擎，让任何羽毛球爱好者到场都能打上势均力敌的球局
- **界面语言**: 中文
- **主题偏好**: user_specified（场地墨绿主色 #14665B、荧光黄强调 #E5C84B、暖白底 #FAF7F0、深墨绿文字 #1A2E2A）
- **导航模式**: 锚点导航
- **导航布局**: Topbar（顶部固定）

---

## 页面结构总览

> **说明**：单页面应用，使用锚点导航在三个主要区块间切换。所有内容在同一个 index.html 文件中，CSS 与 JS 全部内联。

**页面文件**: `index.html`（单文件自包含，原生 HTML/CSS/JS，无外部依赖）

| 区块名称 | 锚点 | 区块说明 |
|---------|------|---------|
| 顶部导航栏 | - | 品牌名 + 锚点链接 + CTA 按钮，固定在顶部 |
| Hero 首屏区 | `#hero` | 大标题 + 副标题 + 双 CTA 按钮 |
| 核心功能亮点区 | `#features` | 4 张功能卡片，每张配手写内联 SVG 图标 |
| 如何加入区 | `#howto` | 3 步加入说明，配 SVG 步骤图标 |
| 在线头像墙 | `#avatar-wall` | 实时模拟在线人数、头像网格动态增删、进出提示条 |
| 临时实时聊天室 | `#chatroom` | 气泡式聊天、匿名昵称、模拟自动回复、消息即发即消 |
| 底部版权区 | - | 版权声明 + 「数据为演示示意」标注 |

---

## 页面布局建议

- **布局模式**: 单栏垂直流式布局 —— 移动端优先，单列居中；桌面端最大宽度约 720px 居中，两侧留白
- **视觉重心**: Hero 区（首屏转化） + 头像墙/聊天室（交互演示区，证明产品活跃度）
- **结果承载区**: 
  - 头像墙：动态头像网格 + 人数计数器 + 进出提示条；初始态为已加载的 20-30 个随机头像
  - 聊天室：消息气泡列表 + 输入框；初始态为系统欢迎消息 + 几条历史模拟消息
- **响应式断点**: ≤420px 为手机形态单列，>420px 桌面端居中限宽

---

## 导航配置

- **导航布局**: Topbar（顶部固定，透明/实色随滚动变化）
- **导航项**:

| 导航文字 | 锚点 | 类型 |
|---------|------|------|
| 羽约 YUYUE | `#hero` | 品牌名 |
| 首页 | `#hero` | 锚点链接 |
| 在线头像墙 | `#avatar-wall` | 锚点链接 |
| 临时聊天室 | `#chatroom` | 锚点链接 |
| 加入球局 | - | CTA 按钮（荧光黄强调色） |

---

## 数据来源声明

| 数据/操作 | 来源类型 | 实现要求 | mock 兜底 |
|---|---|---|---|
| 功能卡片数据 | demo-mock | 内联 JS 常量数组，含标题、描述、SVG 图标 | ✅ 本身就是 mock |
| 加入步骤数据 | demo-mock | 内联 JS 常量数组，含步骤编号、标题、描述、SVG 图标 | ✅ 本身就是 mock |
| 头像墙在线用户 | demo-mock | JS 生成随机用户池（昵称+学院+头像色），定时器随机增删模拟实时性 | ✅ 本身就是前端模拟 |
| 进出提示消息 | demo-mock | 头像增删时同步生成提示文案，顶部轮播展示 | ✅ 本身就是前端模拟 |
| 聊天室用户昵称 | demo-mock | 进入页面时自动生成匿名昵称「球友#xxxx」，存在内存变量中 | ✅ 本身就是 mock |
| 聊天室消息列表 | demo-mock | 用户发送的消息存在内存数组中，其他用户回复从预设回复池随机选取 | ✅ 本身就是前端模拟 |
| 模拟自动回复 | demo-mock | 预设羽毛球相关回复池，定时器/用户发送后随机触发 | ✅ 本身就是前端模拟 |

---

## 功能列表

### 顶部导航栏

- **页面目标**: 提供全局导航入口，引导用户在单页各区块间快速跳转
- **功能点**:
  - **锚点平滑滚动**: 点击导航链接平滑滚动到对应区块，URL hash 同步更新
  - **CTA 按钮**: 「加入球局」按钮使用荧光黄强调色，点击滚动到 Hero 区或弹出引导提示
  - **响应式导航**: 移动端（≤420px）导航项简化，确保不拥挤

### Hero 首屏区

- **页面目标**: 传达产品核心价值，吸引用户继续浏览或立即加入
- **功能点**:
  - **主副标题展示**: 大标题「让每一个羽毛球爱好者，都能打上势均力敌的球局」+ 副标题说明约球撮合 + ELO 战绩 + 自动编排定位
  - **双 CTA 按钮**: 「立即加入」（荧光黄主按钮）+「查看球局」（墨绿描边次按钮），点击分别滚动到对应区域
  - **品牌视觉强化**: 羽毛球主题装饰元素（内联 SVG 羽毛球/球场线），营造运动氛围

### 核心功能亮点区

- **页面目标**: 展示产品四大核心能力，建立用户认知
- **功能点**:
  - **功能卡片展示**: 4 张卡片（发布/加入球局、自动编排引擎、ELO 积分与战绩、实时头像墙与聊天室），白色/浅暖白底、圆角、轻阴影
  - **手写 SVG 图标**: 每张卡片顶部配一个内联 SVG 图标（球拍/球场/奖杯/聊天等），与卡片内容对应
  - **卡片 hover 动效**: 鼠标悬停时轻微上浮 + 阴影加深，增强交互感

### 如何加入区

- **页面目标**: 降低新用户使用门槛，清晰展示 3 步加入流程
- **功能点**:
  - **3 步流程展示**: 微信打开小程序 → 学号+姓名认证 → 浏览球局报名开打，竖向或横向排列
  - **SVG 步骤图标**: 每步配一个手绘风格 SVG 图标（手机/学生证/羽毛球拍）
  - **步骤序号强调**: 使用荧光黄色圆形序号标记，突出步骤感

### 在线头像墙

- **页面目标**: 模拟产品活跃度，展示实时在线的球友氛围
- **功能点**:
  - **实时在线人数**: 数字实时跳动动画，显示当前在线人数（20-30 之间浮动）
  - **头像网格动态增删**: JS 定时器每隔 2-4 秒随机增删一个头像，模拟用户进出；头像用内联 SVG 生成（不同底色 + 人像轮廓）
  - **进场/离场提示条**: 顶部横向滚动/轮播提示条，显示「xxx · 学院 进场/离场」，进场绿色、离场灰色
  - **演示数据标注**: 区块内明显位置标注「演示数据 · 前端模拟」灰色提示

### 临时实时聊天室

- **页面目标**: 展示社区互动氛围，模拟球友间的即时交流
- **功能点**:
  - **匿名昵称生成**: 进入页面/区块时自动生成匿名昵称如「球友#1024」，显示在聊天区顶部
  - **气泡式消息列表**: 自己发的消息靠右（墨绿底白字），其他人消息靠左（浅暖白底深绿字），带头像
  - **消息发送**: 输入框 + 发送按钮，回车或点击按钮发送，消息立即上屏并滚动到底部
  - **模拟自动回复**: JS 定时器在用户发送消息后 1-3 秒内，或随机间隔 8-15 秒，从预设羽毛球相关回复池中随机选取一条，以随机用户身份发送
  - **临时会话标识**: 顶部黄色小徽章「临时会话 · 不保存」，底部注明「消息仅本次会话展示，关闭后不保留任何记录」

### 底部版权区

- **页面目标**: 展示版权信息和数据性质声明
- **功能点**:
  - **版权声明**: 品牌名 + 年份 + 版权所有
  - **演示数据标注**: 明确标注「所有数据为演示示意，非真实在线数据」

---

## 视觉风格规范

### 色彩体系

| 用途 | 色值 | 说明 |
|-----|------|------|
| 主色（场地墨绿） | `#14665B` | 导航栏、按钮、标题、重要文字 |
| 强调色（荧光黄） | `#E5C84B` | CTA 按钮背景、徽章、高亮、序号 |
| 背景色（暖白） | `#FAF7F0` | 页面背景、卡片次要背景 |
| 文字主色（深墨绿） | `#1A2E2A` | 正文、标题 |
| 文字次要色 | `#5A726D` | 辅助说明、次要信息 |
| 卡片背景 | `#FFFFFF` / `#FAF7F0` | 白色主卡片 / 浅暖白次级卡片 |
| 进场提示色 | `#2E8B57` | 头像进场提示（绿色系） |
| 离场提示色 | `#9AA8A4` | 头像离场提示（灰色系） |

### 设计原则

- **移动端优先**: ≤420px 单列居中，内容撑满屏幕（左右留 16px 内边距）
- **桌面端限宽**: >420px 内容最大宽度 720px 居中，两侧留白
- **卡片风格**: 圆角 12px，轻阴影（box-shadow: 0 2px 8px rgba(20, 102, 91, 0.08)）
- **按钮风格**: 圆角 8px，主按钮荧光黄底 + 深墨绿字 + 加粗；次按钮墨绿描边 + 墨绿字
- **图标**: 全部使用手写内联 SVG，禁止 emoji 和外部图标库

### 技术约束

- 单文件 `index.html`，CSS 与 JS 全部内联
- 无任何外部依赖（无 CDN、无框架、无图标库）
- 所有图标使用内联 SVG 手写实现
- 禁止使用 emoji 字符
- JS 定时器模拟实时效果（头像墙增删、聊天室自动回复）
- 响应式适配，移动端优先

-------

<scene_type>prototype-app</scene_type>

# UI 设计指南

## 1. 设计推导依据

- **参考意图**: Free Direction —— 用户提供完整产品需求与明确品牌色，无参考图约束，按羽毛球校园语义自主建立视觉系统
- **核心情绪 / 应用类型**: 面向校园羽毛球爱好者的轻盈运动感 Landing + 轻互动原型；干净、有活力、低门槛
- **独特记忆点**: 羽毛球场地墨绿 + 荧光黄球头配色贯穿全站，头像墙与聊天室的"实时在场感"成为产品核心记忆锚

## 2. Art Direction

- **方向名**: 球场运动极简
- **Design Style**: Swiss Minimalist + Soft Sport —— 从羽毛球场地提取色彩与线条感，用干净网格承载功能叙事，柔和圆角与轻阴影保留校园亲切感
- **DNA 参数**: 圆角 soft (rounded-lg) / 阴影 subtle (shadow-sm + 卡片 hover 轻微上浮) / 间距 standard (gap-4, p-6) / 字体方向 无衬线中文标题+正文，清晰运动感 / 装饰手法 羽毛球场网线条装饰、荧光黄窄条强调、SVG 图标线性风格
- **应用类型**: Landing + Interactive Prototype —— 单页三锚点结构，内容居中单列叙事

## 3. Color System

**色彩关系**: 场地墨绿主色 + 荧光黄强强调色 + 暖白底纸感基底；深墨绿文字保证阅读对比度，浅暖绿作为 hover/选中反馈底
**配色设计理由**: primary 来自羽毛球场地标准墨绿色，承担品牌识别与主行动；accent 荧光黄来自羽毛球球头，用于 CTA 按钮与关键状态高亮，视觉跳脱但用量克制；暖白底模拟纸张/场地周边环境色，减少屏幕冷感
**主色推导**: 以用户指定的场地墨绿 #14665B 为 primary 锚点，深墨绿 #1A2E2A 作为文字主色（加深保证对比度），荧光黄 #E5C84B 作为 accent 球头色，暖白 #FAF7F0 作为 bg，同色系浅绿衍生 border 与 accent 底
**使用比例**: 60% 暖白中性 / 30% 墨绿文字与边框 / 10% 荧光黄强调；荧光黄只用于主 CTA、徽章、关键状态，不用于普通按钮、边框或图标底色

| 角色 | CSS 变量 | Tailwind Class | HSL 值 | 设计说明 |
|---|---|---|---|---|
| bg | `--background` | `bg-background` | hsl(45 33% 96%) | 暖白底 #FAF7F0，页面大背景 |
| card | `--card` | `bg-card` | hsl(0 0% 100%) | 纯白卡片，承载功能块与聊天气泡 |
| text | `--foreground` | `text-foreground` | hsl(168 28% 14%) | 深墨绿 #1A2E2A，标题与正文 |
| textMuted | `--muted-foreground` | `text-muted-foreground` | hsl(168 12% 42%) | 浅墨绿，辅助说明与时间戳 |
| primary | `--primary` | `bg-primary` / `text-primary` | hsl(172 67% 24%) | 场地墨绿 #14665B，品牌识别、导航、次按钮 |
| primaryForeground | `--primary-foreground` | `text-primary-foreground` | hsl(45 33% 96%) | 主色上的文字，暖白保证对比 |
| accent | `--accent` | `bg-accent` | hsl(50 75% 59%) | 荧光黄 #E5C84B，主 CTA 按钮、徽章、高亮态 |
| accentForeground | `--accent-foreground` | `text-accent-foreground` | hsl(168 28% 14%) | 荧光黄上的深墨绿文字，高对比 |
| border | `--border` | `border-border` | hsl(168 18% 88%) | 浅墨绿边，卡片与输入框边界 |

**语义色提示**: 进场提示（成功感）—— bg: hsl(160 40% 92%) / border: hsl(168 35% 65%) / text: hsl(172 67% 24%)，与 primary 同色系偏浅；离场提示（弱化）—— bg: hsl(20 15% 92%) / border: hsl(20 15% 75%) / text: hsl(20 10% 35%)，暖灰调降低权重；黄色徽章用 accent 色系，饱和度与主 accent 对齐。

## 4. 字体与节奏

- **font-display**: Noto Sans SC (700/800) —— 中文无衬线粗体，硬朗运动感，匹配校园产品的年轻活力
- **font-body**: Noto Sans SC (400/500) —— 正文清晰易读，与标题同字体家族保持统一
- **字号**: H1 text-4xl sm:text-5xl；H2 text-2xl sm:text-3xl；body text-base；muted text-sm；移动端适度降 1 级
- **圆角**: 大 (rounded-lg for cards, rounded-full for buttons/avatars) —— 运动感与亲和力兼顾，头像全圆、按钮胶囊、卡片大圆角

## 5. 全局布局契约

- **Reference Layout Use**: 按需求结构推导
- **Page / Section Order**: ① Hero + 功能亮点 + 加入步骤 → ② 在线头像墙 → ③ 临时聊天室；顶部导航锚点平滑滚动
- **Standard Content Zone**: max-w-[720px] + `mx-auto`，桌面端窄内容聚焦阅读，移动端全宽单列
- **Shell / Frame Alignment**: 导航与内容区同宽对齐，移动端导航全宽、内边距与内容一致
- **Padding & Rhythm**: `px-5 md:px-6 py-12 md:py-16`，区块间间距 `space-y-16 md:space-y-20`，卡片内 `p-5`
- **Full-bleed Zones**: Hero 背景可全宽铺墨绿或场地纹理，内部文字与按钮仍受 720px 约束；其余区块背景统一暖白
- **Local Narrowing**: 聊天室与头像墙受同一 720px 约束，不单独收窄
- **Overflow Strategy**: 头像网格自适应换行，聊天消息列表垂直滚动；无横向溢出场景
- **Flexibility Boundary**: 允许移动端卡片 padding 从 p-5 调至 p-4、头像尺寸微调；不允许变更主色、圆角体系、内容区最大宽度

## 6. 视觉与动效

- **装饰**: 羽毛球场网细线分隔、荧光黄窄条标题前缀、线性 SVG 图标（2px 描边）
- **阴影/边界**: 轻 —— 卡片 `shadow-sm`，hover 时 `shadow-md` 轻微上浮；边界用浅墨绿细边
- **动效**: 克制而有反馈 —— 按钮 hover 轻微缩放 + 颜色加深；卡片 hover 微抬升；头像进出用淡入淡出；进场/离场提示从上方滑入后淡出；所有过渡 150-250ms ease-out

## 7. 组件原则

- 按钮分三级：主 CTA 用荧光黄底+深墨绿字（圆角胶囊）；次按钮用墨绿底+暖白字；三级按钮用白底+墨绿边
- 卡片统一白背景、rounded-lg、border-border、shadow-sm；功能卡含 SVG 图标+标题+描述三段式
- 输入框白底、border-border、rounded-lg，focus 时 border-primary + 轻微外发光
- 聊天气泡：自己发的右对齐、墨绿底暖白字；他人发的左对齐、浅暖灰底深墨绿字；均 rounded-2xl
- 加载态与空状态用同色系浅底 + 线性 SVG 图标，保持视觉语言一致

## 8. Image Direction

- **Image Role**: 无强制图片需求 —— 全站视觉记忆点通过配色、排版和手写内联 SVG 图标建立，不使用外部图片或插画
- **Image Art Direction**: 无强制图片需求
- **Image Prompt Keywords**: 无
- **Image Avoidance**: 避免通用运动人物素材图、羽毛球摆拍图库照、无意义渐变背景；如后续需 Hero 图，应走"球场第一视角 + 暖光"的真实场景路线

## 9. Anti-patterns

- **Fluorescent flood**: 荧光黄铺满按钮、图标、边框、链接各处；严格控制在 10% 以内，只用于主 CTA 与关键状态徽章
- **Mobile afterthought**: 先做桌面再硬塞移动端；本产品移动端优先，≤420px 单列居中是默认形态
- **Generic SaaS relapse**: 回退到蓝白配色与通用商务图标；所有视觉必须从羽毛球场地语义（墨绿+荧光黄+球网线条）衍生
- **Chat bubble chaos**: 左右气泡颜色、圆角、间距不统一；自己/他人气泡仅位置和背景色不同，圆角与内边距一致
- **Demo data ambiguity**: 演示数据与真实数据视觉上无区分；头像墙与聊天室必须有显著"演示数据/前端模拟"标注
- **Invisible keyboard**: 按钮与输入框只有 hover 态无 focus-visible 态；所有可交互元素必须有清晰键盘焦点环